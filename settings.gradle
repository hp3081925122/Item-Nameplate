pluginManagement {
    repositories {
        // 使用 NeoForge ModDevGradle 插件所需的官方仓库。
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases' }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'Item_nameplate'
